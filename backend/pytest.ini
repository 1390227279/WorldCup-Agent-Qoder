[pytest]
# 将 backend/ 目录加入 Python 路径，使 `from app.xxx import ...` 可直接生效
pythonpath = .

# 默认扫描目录
testpaths = tests

# asyncio 模式：auto 自动识别 async 测试函数
asyncio_mode = auto

# 测试文件匹配模式
python_files = test_*.py
python_classes = Test*
python_functions = test_*
